{
  "name": "sse-server",
  "version": "1.0.0",
  "description": "Server-Sent Events server with dynamic channels and RESTful API",
  "main": "src/index.js",
  "scripts": {
    "start": "PORT=8000 ALLOWED_ORIGINS='*' node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest"
  },
  "keywords": ["sse", "server-sent-events", "express", "real-time"],
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
